#pragma once

enum class SCENEID
{
	LOGO,
	MENU,
	HELPSCENE,
	PLAYERSELECTSCENE,
	STAGE,
	EXIT,
};

enum class MENUID
{
	ZERO,
	START,
	HELP,
	EXIT,
};

enum class BULLETID
{
	ZERO,
	PLAYER,
	ENEMY,
};

enum class ENEMYID
{
	ZERO,
	NOMAL,
	BOOMB,
	BOSS,
};

enum class DIRECTION
{
	LEFT,
	MID,
	RIGHT,
};
	